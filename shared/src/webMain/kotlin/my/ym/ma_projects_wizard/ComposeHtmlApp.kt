package my.ym.ma_projects_wizard

/**
 * @return `true` if it can be rendered (in case of js) or `false` if can't be rendered (in case of wasmJs) Inshallah.
 */
expect fun renderComposeHtmlApp(): Boolean
